import React from 'react';
import Header from '../../components/common/Header';

const FlexSearch = () => {
    return (
        <div>
            <Header flex_search />
        </div>
    );
};

export default FlexSearch;